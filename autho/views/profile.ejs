<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
   
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class ="w-full flex justify-end"><a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a></div>
         <h3 class="text-3xl mb-5">Hello, <%= user.username %> </h3><!--getting data from app.js -->
       <h5 class="mb-5">you can create a post.</h5>
            <form action="/post" method="post">
                <textarea placeholder="whatever you want to write" class=" w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
                <input class="px-3 py-2 bg-blue-900 block rounded-md mt-2 border-2 border-zinc-800" type="submit" value="Create your post"></input>
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.forEach(function(post){ %>
                <div class="post w-1/3 p-4 border-2 border-zinc-800 bg-red">
                <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                <p class="text-sm tracking-tight"><%= post.content %></p>
                <small class="mt-2 inline_block"><%=post.likes.length %> likes</small>
                <div class="btns flex gap-4 mt-5">
                    <a class="text-blue-500"href="/like/<%= post._id %>">
                        <%= post.likes.indexOf(user._id) ? "Like" : "Unlike" %>
                    </a>
                    <a  class="text-blue-500"href="/edit/<%= post._id %>">Edit</a>
                </div>
            </div>
            <% }); %>
        </div>
    
</body>
</html>


